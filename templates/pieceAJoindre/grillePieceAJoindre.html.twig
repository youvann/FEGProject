{% extends 'intranet.html.twig' %}

{% block content %}

    <table class="table table-hover table-bordered table-striped">
        <tr>
            <th>Code étape</th>
            <th>Code VET</th>
            <th>Pièce à joindre</th>
            <th>Options</th>
        </tr>
        {% for pieceAJoindre in piecesAJoindre %}
            <tr>
                <td>{{ pieceAJoindre.codeEtape }}</td>
                <td>{{ pieceAJoindre.codeVet }}</td>
                <td>{{ pieceAJoindre.libelPiece }}</td>
                <td><div class="btn-group">
                        <button type="button" class="btn btn-success">Consulter</button>
                        <button type="button" class="btn btn-primary">Modifier</button>
                        <button type="button" class="btn btn-danger"><span class="glyphicon glyphicon-trash"></span> Supprimer</button>
                    </div>
                </td>
            </tr>
        {% endfor %}
    </table>

{% endblock %}