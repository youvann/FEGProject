{% extends 'layout.html.twig' %}

{% block content %}

    <div class="row" style="margin-top: 1em;">
        <div class="col-md-12">
            <h2 class="titre_rubrique">Récapitulatif</h2>
            <div class="alert alert-success">Votre demande de {{ typeDossier | lower }} a bien été prise en compte. Veuillez conserver votre <b>numéro d'inscription</b> : {{ idEtudiant }} .</div>
            <div class="alert alert-warning">
                Veuillez <b>conserver</b> le dossier de candidature PDF ci-dessous. Ce document contient des informations sur les modalités d'inscription.
                <br/>(Si le PDF ne s'affiche pas correctement, veuillez recharger la page)
            </div>

            <a href="{{ pathPdf }}.pdf" download class="btn btn-info btn-block">
                <span class="glyphicon glyphicon-cloud-download"></span> Télécharger le dossier de {{ typeDossier | lower }}
            </a>

            <br/><br/>

            <div id="loading" style="text-align: center;"></div>
            <object id="pdfPreview" data="{{ pathPdf }}.pdf" type="application/pdf"
                    width="100%"
                    height="700px">
                <p>Si le PDF ne s'affiche pas, veuillez recharger la page.</p>
            </object>

        </div>
    </div>
{% endblock %}

{% block scripts %}
    {{ parent() }}
    <script type="text/javascript">
        $(document).ready(function () {
            $('#pdfPreview').hide();
            $('#loading').append(
                    $('<img/>').attr('src', 'public/img/layout/ajax-loader.gif')
            ).append($('<div/>').text('Veuillez patienter ...'));
            setTimeout(function () {
                $('#loading').fadeOut('fast');
                $('#pdfPreview').fadeIn('slow');
            }, 1000);
        });
    </script>
{% endblock %}
